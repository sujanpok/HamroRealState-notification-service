// import admin from '../config/firebase.js';

// export async function sendNotification({ deviceToken, notification, data = {} }) {
//   const message = {
//     token: deviceToken,
//     notification,
//     data,
//   };
//   return admin.messaging().send(message); // Use new FCM send() API
// }
